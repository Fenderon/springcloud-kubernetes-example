spring:
  application:
    name: consumer-service
  cloud:
    kubernetes:
      client:
        namespace: my-service
    discovery:
      enabled: true
feign:
  okhttp:
    enabled: true

server:
  port: 8080
